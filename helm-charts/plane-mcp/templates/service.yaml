apiVersion: v1
kind: Service
metadata:
  name: {{ include "plane-mcp.fullname" . }}
  labels:
    {{- include "plane-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "plane-mcp.selectorLabels" . | nindent 4 }}
