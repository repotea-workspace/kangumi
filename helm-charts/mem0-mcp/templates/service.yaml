apiVersion: v1
kind: Service
metadata:
  name: {{ include "mem0-mcp.fullname" . }}
  labels:
    {{- include "mem0-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mem0-mcp.selectorLabels" . | nindent 4 }}
