apiVersion: v1
kind: Service
metadata:
  name: {{ include "obsidian-fns.fullname" . }}
  labels:
    {{- include "obsidian-fns.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "obsidian-fns.selectorLabels" . | nindent 4 }}
