Fast Note Sync Service is available inside the cluster at:

  http://{{ include "obsidian-fns.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.service.port }}

Health check:

  http://{{ include "obsidian-fns.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.service.port }}/api/health
